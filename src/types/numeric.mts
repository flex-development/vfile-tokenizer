/**
 * @file Type Aliases - Numeric
 * @module fsm-tokenizer/types/Numeric
 */

/**
 * A string that can be parsed to a valid number.
 */
type Numeric = `${number}`

export type { Numeric as default }
