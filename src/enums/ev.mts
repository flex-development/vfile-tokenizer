/**
 * @file Enums - ev
 * @module fsm-tokenizer/enums/ev
 */

import type { EventType } from '@flex-development/fsm-tokenizer'

/**
 * Event types.
 *
 * @enum {EventType}
 */
enum ev {
  enter = 'enter',
  exit = 'exit'
}

export default ev
